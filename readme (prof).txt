-open in the folder "Application ecommerce" 
-than open in terminale the folder "api" and execute the commande {mvn spring-boot:run}
-than open in terminale the folder "front" and execute the commande {npm run dev}
-the browse to the link "http://localhost:5173/"


-the password and login for the admin is ["admin" , "admin123"]